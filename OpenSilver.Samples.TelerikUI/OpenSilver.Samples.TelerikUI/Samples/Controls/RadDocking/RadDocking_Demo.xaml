<UserControl x:Class="OpenSilver.Samples.TelerikUI.RadDocking_Demo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:OpenSilver.Samples.TelerikUI.Docking">

    <UserControl.Resources>
        <telerik:InvertedBooleanConverter x:Key="InvertedBooleanConveter" />
        <SolidColorBrush x:Key="LightGrayBrush"
                         Color="#FFEFEFF2" />
        <SolidColorBrush x:Key="LightGrayBackgroundBrush"
                         Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="DarkGrayBrush"
                         Color="#FF424242" />
        <SolidColorBrush x:Key="ContentForegroundBrush"
                         Color="#FF020202" />
        <SolidColorBrush x:Key="GreenBrush"
                         Color="#FF388B41" />
        <SolidColorBrush x:Key="DisableBrush"
                         Color="#4C434647" />
        <SolidColorBrush x:Key="LabelBrush"
                         Color="#767676" />
        <SolidColorBrush x:Key="ToolbarForegroundBrush"
                         Color="#FF434647" />

        <Style TargetType="telerik:RadTreeViewItem">
            <Setter Property="FontSize"
                    Value="13" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
        </Style>

        <Style TargetType="telerik:RadToolBar">
            <Setter Property="Height"
                    Value="28" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
        </Style>

        <Style TargetType="telerik:RadPane">
            <Setter Property="CanDockInDocumentHost"
                    Value="False" />
            <Setter Property="Header"
                    Value="{Binding Header}" />
            <Setter Property="IsHidden"
                    Value="{Binding IsHidden, Mode=TwoWay}" />
            <Setter Property="IsActive"
                    Value="{Binding IsActive, Mode=TwoWay}" />
        </Style>

        <Style TargetType="telerik:RadDocumentPane">
            <Setter Property="Header"
                    Value="{Binding Header}" />
            <Setter Property="IsHidden"
                    Value="{Binding IsHidden, Mode=TwoWay}" />
            <Setter Property="IsActive"
                    Value="{Binding IsActive, Mode=TwoWay}" />
        </Style>

        <Style x:Key="ViewMenuItemStyle"
               TargetType="telerik:RadMenuItem">
            <Setter Property="IsCheckable"
                    Value="True" />
            <Setter Property="StaysOpenOnClick"
                    Value="True" />
            <Setter Property="Header"
                    Value="{Binding Header}" />
            <Setter Property="IsChecked"
                    Value="{Binding IsHidden, Mode=TwoWay, Converter={StaticResource InvertedBooleanConveter}}" />
        </Style>

        <Style x:Key="WindowMenuItemStyle"
               TargetType="telerik:RadMenuItem"
               BasedOn="{StaticResource ViewMenuItemStyle}">
            <Setter Property="IsCheckable"
                    Value="True" />
            <Setter Property="Header"
                    Value="{Binding Header}" />
            <Setter Property="IsChecked"
                    Value="{Binding IsActive, Mode=TwoWay}" />
        </Style>

        <CollectionViewSource  x:Key="activeViewsSource"
                               Source="{Binding Panes}"
                               Filter="FilterActiveViewsSource" />
        <CollectionViewSource  x:Key="toolboxesSource"
                               Source="{Binding Panes}"
                               Filter="FilterToolboxesSource" />
    </UserControl.Resources>

    <ContentControl Style="{StaticResource Card_Style}"
                    Height="500"
                    Width="950">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ContentControl Content="RadDocking"
                            Foreground="#FFD66DED"
                            Background="#FFBA8AC6"
                            Style="{StaticResource HeaderControl_Style}" />
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <telerik:RadMenu VerticalAlignment="Top">
                    <telerik:RadMenuItem Header="File">
                        <telerik:RadMenuItem Header="New document"
                                             Command="{Binding NewDocumentCommand}" />
                        <telerik:RadMenuItem Header="Save"
                                             Command="{Binding SaveCommand}"
                                             CommandParameter="{Binding ElementName=radDocking}" />
                        <telerik:RadMenuItem Header="Load"
                                             Command="{Binding LoadCommand}"
                                             CommandParameter="{Binding ElementName=radDocking}" />
                    </telerik:RadMenuItem>

                    <telerik:RadMenuItem Header="View"
                                         ItemContainerStyle="{StaticResource ViewMenuItemStyle}"
                                         ItemsSource="{Binding Source={StaticResource toolboxesSource}}">
                    </telerik:RadMenuItem>

                    <telerik:RadMenuItem Header="Window"
                                         x:Name="WindowRadMenu"
                                         ItemsSource="{Binding Source={StaticResource activeViewsSource}}"
                                         ItemContainerStyle="{StaticResource WindowMenuItemStyle}">
                    </telerik:RadMenuItem>
                </telerik:RadMenu>

                <telerik:RadDocking x:Name="radDocking"
                                    PreviewShowCompass="OnPreviewShowCompass"
                                    PanesSource="{Binding Panes}"
                                    Close="OnClose"
                                    Grid.Row="1"
                                    Margin="0 0 0 10"
                                    BorderThickness="0"
                                    Padding="0"
                                    Background="{StaticResource LightGrayBrush}">
                    <telerik:RadDocking.DockingPanesFactory>
                        <local:CustomDockingPanesFactory />
                    </telerik:RadDocking.DockingPanesFactory>
                    <telerik:RadDocking.CurrentSaveLoadLayoutHelper>
                        <local:CustomSaveLoadLayoutHelper />
                    </telerik:RadDocking.CurrentSaveLoadLayoutHelper>

                    <telerik:RadSplitContainer MaxWidth="600"
                                               InitialPosition="DockedLeft">
                        <telerik:RadPaneGroup Name="leftGroup"
                                              telerik:RadDocking.SerializationTag="leftGroup">
                        </telerik:RadPaneGroup>
                    </telerik:RadSplitContainer>

                    <telerik:RadSplitContainer MaxWidth="679"
                                               InitialPosition="DockedRight">
                        <telerik:RadPaneGroup x:Name="rightGroup"
                                              telerik:RadDocking.SerializationTag="rightGroup">
                        </telerik:RadPaneGroup>
                    </telerik:RadSplitContainer>

                    <telerik:RadSplitContainer InitialPosition="DockedBottom">
                        <telerik:RadPaneGroup x:Name="bottomGroup"
                                              telerik:RadDocking.SerializationTag="bottomGroup">
                        </telerik:RadPaneGroup>
                    </telerik:RadSplitContainer>
                </telerik:RadDocking>
            </Grid>
            <Button Grid.Row="2"
                    Content="VIEW SOURCE..."
                    Click="ButtonViewSource_Click"
                    Background="#FFD66DED"
                    Style="{StaticResource ButtonViewSource_Style}" />
        </Grid>
    </ContentControl>
</UserControl>
