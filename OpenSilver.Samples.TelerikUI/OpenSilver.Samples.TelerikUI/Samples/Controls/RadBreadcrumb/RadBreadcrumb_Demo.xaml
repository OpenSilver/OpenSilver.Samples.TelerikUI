<UserControl x:Class="OpenSilver.Samples.TelerikUI.RadBreadcrumb_Demo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">

    <ContentControl Style="{StaticResource Card_Style}"
                    Height="140"
                    Width="500">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ContentControl Content="RadBreadcrumb"
                            Foreground="#FFFF657F"
                            Background="#FFCB8591"
                            Style="{StaticResource HeaderControl_Style}" />
            <Grid Grid.Row="1"
                  Margin="15">
                <telerik:RadBreadcrumb VerticalAlignment="Top"
                                       Header="{Binding Root}"
                                       ImagePath="IconPath"
                                       IsIconVisible="True"
                                       ItemsSource="{Binding Root.Children}"
                                       TextModePath="Path">
                    <telerik:RadBreadcrumb.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Header}" />
                        </DataTemplate>
                    </telerik:RadBreadcrumb.HeaderTemplate>
                    <telerik:RadBreadcrumb.ItemTemplate>
                        <telerik:HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <telerik:HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Header}" />
                                </DataTemplate>
                            </telerik:HierarchicalDataTemplate.ItemTemplate>
                            <TextBlock Text="{Binding Header}" />
                        </telerik:HierarchicalDataTemplate>
                    </telerik:RadBreadcrumb.ItemTemplate>
                </telerik:RadBreadcrumb>
            </Grid>

            <Button Grid.Row="2"
                    Content="VIEW SOURCE..."
                    Click="ButtonViewSource_Click"
                    Background="#FFFF657F"
                    Style="{StaticResource ButtonViewSource_Style}" />
        </Grid>
    </ContentControl>

</UserControl>
